// select an element with id parent-node
let parentNode = document.getElementById('parent-node')
// document.querySelector('#parent-node')
// document.body.firstElementChild

// select the child node of parentNode
// let childNode = 
// parentNode.firstElementChild
// parentNode.querySelector('div')

// change to h1 text 
// parentNode.querySelector('h1').innerText = "This is DOM Manipulation"

// create a paragraph element & append to parentnode
    // create a paragraph
    // let p = document.createElement('p')

    // // append to parentNode
    // parentNode += p 
    
